model Pdf {
  id          String   @id @default(cuid())
  title       String
  description String?
  fileUrl     String
  fileSize    String?
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt

  @@index([title])
}
