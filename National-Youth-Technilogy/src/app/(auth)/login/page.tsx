import LoginPage from '@/components/Authentication/auth/login-form';
import React from 'react';

const page = () => {
    return (
        <div>
            <LoginPage />
        </div>
    );
};

export default page;